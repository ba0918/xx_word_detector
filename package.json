{
  "name": "@ba0918/xx_word_detector",
  "description": "detect NG word from text",
  "version": "0.0.2",
  "repository": "git@github.com:ba0918/xx_word_detector.git",
  "author": "ba0918 <pub.dev.delusions@gmail.com>",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "run-s build:rm build:mkdir build:cp build:js",
    "build:js": "babel src --out-dir dist",
    "build:cp": "cpx {package.json,README.md} dist",
    "build:mkdir": "mkdirp dist",
    "build:rm": "rimraf dist",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^0.24.0",
    "cross-env": "^5.0.0",
    "eslint": "^4.14.0",
    "eslint-config-prettier": "^2.1.0",
    "eslint-plugin-prettier": "^2.1.1",
    "npm-run-all": "^4.0.2",
    "prettier": "^1.3.1",
    "rimraf": "^2.6.1",
    "typescript": "^2.6.2"
  },
  "ava": {
    "files": [
      "test/**/*.test.js"
    ],
    "require": [
      "babel-register"
    ]
  }
}
